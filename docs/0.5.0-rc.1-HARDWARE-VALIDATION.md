# BootCamp Performance Control 0.5.0-rc.1 Hardware Validation Record

Date: 2026-09-05

## Purpose

This record preserves the physical validation performed immediately before preparing the `0.5.0-rc.1` release candidate and the final publication identity of the candidate created from that validated runtime.

The validated runtime was the Phase B code merged to `main` at:

```text
5a041303c67175491a9f36ff1927db8c5484ec30
```

The release-preparation change to `0.5.0-rc.1` changed version metadata and public documentation only. It did not modify the hardware-control implementation that was exercised below.

The final qualified release-preparation source was:

```text
27511afee7e1ae092bb53e63d8c1c96b73004c81
```

That source is the commit referenced by annotated tag `v0.5.0-rc.1`.

This record validates the reference machine only. It must not be interpreted as physical validation of every Apple T2 Mac.

## Published release-candidate identity

`0.5.0-rc.1` was published on 2026-09-05 as a GitHub pre-release.

```text
Tag:           v0.5.0-rc.1
Tag target:    27511afee7e1ae092bb53e63d8c1c96b73004c81
Release type:  pre-release
ZIP:           BootCampPerformanceControl-0.5.0-rc.1-win-x64.zip
ZIP size:      70320423 bytes
ZIP SHA-256:   B2215F7C6846614F2F1606A5DC11DC2D0BB1A496C66ACBA523B607A8DC65DDD5
SHA sidecar:   BootCampPerformanceControl-0.5.0-rc.1-win-x64.zip.sha256
```

Stable `v0.4.0` was not moved or replaced and remains the project's latest stable release.

The post-release documentation synchronization that updates this record occurs **after** the immutable `v0.5.0-rc.1` tag. It does not change the released source or artifact.

## Test machine

```text
Model:          MacBookPro16,1
Product:        MacBook Pro 16-inch (2019)
CPU:            Intel Core i9-9980HK
GPU:            AMD Radeon Pro 5500M
Platform:       Apple T2 / Intel Mac
OS:             Windows 10 Pro Boot Camp
Windows build:  19045, 64-bit
```

Production fan interoperability used the separately installed AppleSMC compatibility driver from the previously validated Macs Fan Control environment. The Macs Fan Control application itself was closed during BCPC ownership tests.

```text
Macs Fan Control: 1.5.16 (Build 693)
AppleSMC driver:  applesmc.sys
Driver FileVersion: 1.0.7.0
```

BCPC did not redistribute or install these third-party files during the test.

The experimental native `drivers/BootCampSmc` driver was not used for the production fan-control validation.

## Automated qualification before hardware testing

Phase B candidate qualification reported:

```text
Debug tests:            589 / 589 PASS
Release build:          PASS, 0 warnings / 0 errors
Release tests:          589 / 589 PASS
git diff --check:       PASS
PR #42 Windows CI:      PASS
Post-merge main CI:     PASS
```

PR #42 merged the dynamic topology / verified T2-family write path to `main` before physical validation was completed.

## Release-preparation qualification

The version/documentation release-preparation path was separately qualified before publication:

```text
Release version:        0.5.0-rc.1
Release tests:          589 / 589 PASS
Release build:          PASS, 0 warnings / 0 errors
Packaging validation:   PASS
Post-merge main CI:     PASS
Release safety scan:    PASS
```

The CI packaging run and the final locally published ZIP were both built from the same qualified source commit. Their ZIP byte hashes are not expected to be identical because ZIP container timestamps can differ between independent package runs. The published release digest recorded above is the authoritative digest for the downloadable RC artifact.

## Gate HW-A — read-only capability preflight

No BCPC fan writes had been performed in this session when this baseline was captured.

Observed application state:

```text
Platform support:  Supported Intel Mac
Model validation:  Performance validated
AppleSMC backend:  Running
Safety status:     Read-only monitoring verified
Mode:              Apple Auto
Write control:     Available (verified T2 SMC family)
Restore snapshot:  Not available
CPU Maximum AC:    100%
CPU Maximum DC:    100%
Boost AC:          2 (Aggressive)
Boost DC:          2 (Aggressive)
```

Observed fans:

```text
Fan 0: 5036 / 5616 RPM
Fan 1: 4658 / 5200 RPM
```

Result: **PASS**.

This demonstrated live recognition of the expected MMIO / dynamic two-fan FLT4 family on the reference machine while the fans were still Apple-managed.

## Gate HW-B — Gaming Optimised apply and normal Restore

Gaming Optimised was applied once through the production WPF application.

Observed post-apply state:

```text
Detected profile state:
Gaming Optimised is fully active: CPU settings and Maximum Safe RPM fans are verified.

Restore snapshot:
Available - original processor settings can be restored.

CPU Maximum AC: 95%
CPU Maximum DC: 95%
Boost AC:       0 (Disabled)
Boost DC:       0 (Disabled)
Mode:           Manual
Write control:  Maximum Safe RPM detected (Manual mode)
```

Observed fans:

```text
Fan 0: 5587 / 5616 RPM
Fan 1: 5208 / 5200 RPM
```

The commanded target for Fan 1 remained its fresh live maximum of `5200 RPM`; the `5208 RPM` value was the instantaneous actual read-back and was within the bounded runtime tolerance.

Result: **PASS**.

`Restore Original Settings` was then executed.

Observed post-Restore state:

```text
CPU Maximum AC: 100%
CPU Maximum DC: 100%
Boost AC:       2 (Aggressive)
Boost DC:       2 (Aggressive)
Restore snapshot: Not available
Mode:           Apple Auto
Write control:  Available (verified T2 SMC family)
```

Observed fan values while Apple Auto was already reducing speed:

```text
Fan 0: 3297 / 5616 RPM
Fan 1: 3028 / 5200 RPM
```

Result: **PASS**.

This verified the production Apply / read-back / `FANS -> POWER` Restore round-trip on physical T2 hardware.

## Gate HW-C — forced-process crash and startup fan recovery

Gaming Optimised was applied again and verified active. The exact development-build `BootCampPerformanceControl` process was then terminated forcefully so graceful shutdown/Restore could not execute.

After restarting BCPC, observed state was:

```text
Detected profile state:
Gaming CPU settings are active. Fans are currently using Apple Auto.

Restore snapshot:
Available - original processor settings can be restored.

CPU Maximum AC: 95%
CPU Maximum DC: 95%
Boost AC:       0 (Disabled)
Boost DC:       0 (Disabled)
Mode:           Apple Auto
Write control:  Available (verified T2 SMC family)
```

Observed fans after startup recovery:

```text
Fan 0: 1836 / 5616 RPM
Fan 1: 1709 / 5200 RPM
```

Result: **PASS**.

This verified that:

- BCPC fan ownership/recovery context survived forced process termination,
- startup recovery returned owned fans to Apple Auto,
- Apple Auto was verified before ownership cleanup,
- the processor remained intentionally in the Gaming state,
- the original processor Restore snapshot survived the crash and fan recovery.

## Gate HW-D — fan-only resume and snapshot preservation

Starting from the valid split state:

```text
CPU:      95 / 95
Boost:    0 / 0
Snapshot: original processor Restore snapshot still available
Fans:     Apple Auto
```

Gaming Optimised was activated again.

The operator confirmed that fan-only resume returned the fans to the expected Manual / Maximum Safe RPM state without replacing the original processor Restore snapshot.

A subsequent explicit `Restore Original Settings` returned the reference machine to its original processor state and Apple Auto.

Result: **PASS**.

This confirms the critical invariant that fan-only resume does not rewrite the processor profile or replace the Restore snapshot created before the original Gaming transaction.

## Physically validated lifecycle summary

```text
Read-only capability discovery          PASS
Dynamic FNum=2 topology                 PASS
Verified T2/MMIO/FLT4 family gate       PASS
Maximum Safe RPM planning               PASS
Manual fan takeover                     PASS
Per-fan target write/read-back          PASS
CPU 95/95 + Boost 0/0                   PASS
Normal FANS -> POWER Restore             PASS
Forced-process crash persistence         PASS
Startup fan-only Apple Auto recovery     PASS
Processor Gaming-state preservation      PASS
Processor Restore-snapshot preservation  PASS
Fan-only resume                          PASS
Final exact Restore                      PASS
```

## Safety boundaries preserved

No validation step changed the production safety boundary:

- targets came from fresh live `F{i}Mx` values,
- no arbitrary SMC key was written,
- no minimum-RPM write was used,
- no user-defined RPM was used,
- no `FS!` / T1 `fpe2` write was used,
- no CPU undervolting or MSR write was used,
- no firmware or display timing was changed,
- no third-party driver was bundled or redistributed,
- native experimental `BootCampSmc` was not used for production control.

## Release interpretation

This evidence is sufficient to treat the `MacBookPro16,1` Phase B path as physically validated end-to-end and to publish `0.5.0-rc.1` for broader controlled testing.

It is **not** sufficient to promote the project to `1.0`, to declare all T2 models physically validated, or to enable T1 fan writes.

For another machine, the next safe sequence remains read-only capability capture first, then controlled write/read-back/Apple Auto Restore only if the live family fingerprint passes.
